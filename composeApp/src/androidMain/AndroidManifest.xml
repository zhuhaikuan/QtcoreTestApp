<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE"/>
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
    <!-- Required for using ActionCore -->
    <uses-permission android:name="com.motorola.permission.ACCESS_ACTION_CORE" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-sdk tools:overrideLibrary="com.motorola.actioncore.sdk"/>
    <uses-permission android:name="com.lenovo.qtcore.BIND_SERVICE" />

    <permission
        android:name="com.lenovo.quantum.testapp.permission.READ_PDF_DOCUMENTS"
        android:protectionLevel="normal" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.Material.Light.NoActionBar">
        <activity
            android:exported="true"
            android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <provider
            android:name="com.lenovo.quantum.test.PdfDocumentProvider"
            android:authorities="${applicationId}.pdfdocumentprovider"
            android:exported="true"
            android:enabled="true"
            android:permission="com.lenovo.quantum.testapp.permission.READ_PDF_DOCUMENTS"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/document_file_paths" />
        </provider>

        <profileable android:shell="true"/>
    </application>

</manifest>